/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['vivid.api.mdmehedihasan712277.cyou'], // Add your image domain here
    },
};

export default nextConfig;
